# 微信内 H5 + 公众号网页授权接入信息填写模板

## 一、使用说明

这份模板用于确认本项目接入 **微信内 H5 + 公众号网页授权** 时所需的关键信息。

请直接在每一项后面填写。

> 注意
> - `AppSecret`、服务端密钥、数据库密码不要写进前端代码仓库。
> - 建议只把这份文档作为需求确认稿，真实密钥后续通过环境变量提供。

---

## 二、最终接入方案确认

### 2.1 路线确认

- 接入路线：`微信内 H5 + 公众号网页授权`
- 是否确认采用该路线：`是 / 否`
- 负责人：
- 确认日期：

### 2.2 项目端划分

- 管理员网页 Web 端：`是 / 否`
- 管理员 H5 签到码页：`是 / 否`
- 用户微信内 H5 签到页：`是 / 否`
- 用户中奖查询页：`不要 / 要`
- 用户分享海报页：`不要 / 要`

---

## 三、域名与部署信息

### 3.1 线上访问域名

- 线上主域名：
- 是否已启用 HTTPS：`是 / 否`
- 证书状态：`已配置 / 未配置 / 待申请`

### 3.2 页面地址规划

- 管理员后台地址：
- 管理员 H5 签到码页地址：
- 用户签到页地址：
- 大屏抽奖页地址：
- WebSocket 服务地址：

### 3.3 公众号网页授权配置

- 网页授权回调域名：
- 是否已在公众号后台配置：`是 / 否`
- JS 接口安全域名：
- 是否已在公众号后台配置：`是 / 否`
- 业务域名：
- 是否已在公众号后台配置：`是 / 否`

---

## 四、公众号账号信息

### 4.1 基础信息

- 公众号名称：
- 公众号原始 ID：
- 公众号类型：`服务号 / 订阅号`
- 是否已认证：`是 / 否`

### 4.2 开发信息

- 公众号 AppID：
- 公众号 AppSecret：
- Token：
- EncodingAESKey：

### 4.3 接口权限现状

- 是否具备网页授权能力：`是 / 否 / 不确定`
- 是否具备获取用户基本信息能力：`是 / 否 / 不确定`
- 是否需要接入微信 JS-SDK：`是 / 否`
- 是否需要使用微信分享能力：`是 / 否`

---

## 五、服务端接入信息

### 5.1 后端运行信息

- 后端部署方式：`Node.js / Java / 其他`
- 后端运行域名：
- 后端运行端口：
- 是否已有线上环境：`是 / 否`
- 是否已有测试环境：`是 / 否`

### 5.2 环境变量准备

请确认以下内容后续是否通过环境变量提供：

- `WECHAT_APP_ID`：`是 / 否`
- `WECHAT_APP_SECRET`：`是 / 否`
- `WECHAT_REDIRECT_URI`：`是 / 否`
- `SERVER_BASE_URL`：`是 / 否`
- `DATABASE_URL`：`是 / 否`

---

## 六、用户身份与授权规则

### 6.1 授权策略

- 网页授权 scope：`snsapi_base / snsapi_userinfo / 待定`
- 是否要求拿到微信昵称：`是 / 否`
- 是否要求拿到微信头像：`是 / 否`
- 如果用户拒绝授权，是否允许手填昵称继续签到：`是 / 否`

### 6.2 身份标识规则

- 用户唯一身份采用：`openid / unionid / 自定义 userId / 待定`
- 是否需要跨活动保留同一用户身份：`是 / 否`
- 是否允许同一微信号重复签到：`是 / 否`
- 是否允许同一微信号在同一活动重复签到：`是 / 否`

### 6.3 用户资料展示规则

- 大屏签到墙显示字段：`头像 / 昵称 / 签到时间 / 其他`
- 管理员后台显示字段：`头像 / 昵称 / openid / 签到时间 / 其他`
- 若没有头像，是否使用昵称首字作为默认头像：`是 / 否`

---

## 七、活动流程确认

### 7.1 现场签到流程

- 用户到场后是否必须先扫码签到才能参与抽奖：`是 / 否`
- 扫码后是否必须在微信内完成授权：`是 / 否`
- 授权后是否还允许用户手动修改昵称：`是 / 否`
- 授权后是否允许用户上传自定义头像覆盖微信头像：`是 / 否`

### 7.2 大屏同步流程

- 签到成功后，大屏是否立即显示签到头像：`是 / 否`
- 大屏是否显示最近签到用户列表：`是 / 否`
- 大屏是否显示总签到人数：`是 / 否`
- 抽奖候选池是否只包含已签到用户：`是 / 否`

### 7.3 中奖流程

- 中奖结果是否只在管理员网页端和大屏展示：`是 / 否`
- 主持人是否现场口播中奖昵称：`是 / 否`
- 用户端是否不提供中奖查询：`是 / 否`
- 兑奖方式：`现场兑奖 / 中场兑奖 / 活动结束后兑奖 / 其他`

---

## 八、数据存储与日志

### 8.1 数据库信息

- 当前数据库类型：`MySQL / PostgreSQL / SQLite / 文件存储 / 暂无`
- 数据库连接方式：
- 是否已有用户表：`是 / 否`
- 是否已有活动表：`是 / 否`
- 是否已有签到记录表：`是 / 否`
- 是否已有中奖记录表：`是 / 否`

### 8.2 日志与审计

- 是否需要记录网页授权日志：`是 / 否`
- 是否需要记录签到日志：`是 / 否`
- 是否需要记录抽奖日志：`是 / 否`
- 日志保留时长：

---

## 九、验收口径

请确认以下目标是否作为本阶段验收标准：

- 用户在微信内打开签到页可完成网页授权：`是 / 否`
- 用户授权后可自动获取微信昵称和头像：`是 / 否`
- 用户拒绝授权时可手填昵称签到：`是 / 否`
- 签到成功后，管理员后台实时看到新增用户：`是 / 否`
- 签到成功后，大屏签到墙实时刷新：`是 / 否`
- 抽奖时只从已签到用户中抽取：`是 / 否`
- 中奖结果仅在管理员端和大屏展示：`是 / 否`

---

## 十、需要你补充给我的关键信息

请优先补充这 8 项，补齐后就可以开始正式接微信授权：

1. 公众号 `AppID`
2. 公众号 `AppSecret`
3. 线上 `HTTPS` 域名
4. 网页授权回调域名
5. JS 接口安全域名
6. 用户唯一身份采用 `openid` 还是 `unionid`
7. 用户拒绝授权时是否允许手填昵称
8. 数据库存储方案

---

## 十一、官方文档入口

以下为本方案最相关的微信官方文档入口：

- 微信公众号开发概述  
  https://developers.weixin.qq.com/doc/offiaccount/Getting_Started/Overview.html

- 微信网页授权获取用户信息  
  https://developers.weixin.qq.com/doc/offiaccount/OA_Web_Apps/Wechat_webpage_authorization.html

- 微信网页开发 JS-SDK 说明  
  https://developers.weixin.qq.com/doc/offiaccount/OA_Web_Apps/JS-SDK.html

- 微信 OAuth2 授权地址说明  
  https://open.weixin.qq.com/connect/oauth2/authorize

---

## 十二、填写后返回建议格式

建议你填写后直接把下面这段发给我：

```text
公众号 AppID：
公众号 AppSecret：
线上 HTTPS 域名：
网页授权回调域名：
JS 接口安全域名：
用户唯一身份采用：
拒绝授权是否允许手填昵称：
数据库方案：
```
